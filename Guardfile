# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{\.(hbs)})
  watch(%r{partials/.+\.(hbs)})
  # watch('default.hbs')
  watch(%r{assets/css/.+\.(css)})
  watch(%r{assets/js/.+\.(js)})
end

guard :compass